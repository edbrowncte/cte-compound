import assert from "node:assert/strict";
import fs from "node:fs";
import { __indicatorOnlyTest } from "../src/engine-indicator-only.js";
import { __indicatorOnlyUnitsTest } from "../src/engine-indicator-only-units.js";
import { __indicatorOnlyDualTest } from "../src/engine-indicator-only-dual.js";

const {normalizeIndicatorOnly,indicatorOnlyFingerprint,indicatorOnlyCadenceMs,indicatorOnlySettings}=__indicatorOnlyTest;
const {normalizeIndicatorOnlyUnits,ioClientOrderId}=__indicatorOnlyUnitsTest;
const {normalizeTicket,normalizeTickets,structuralFingerprint,eventIsFresh,eventObservedAt,ticketCadenceMs}=__indicatorOnlyDualTest;

const normalized=normalizeIndicatorOnly({enabled:true,pair:"EUR_NZD",timeframe:"S5",indicator:"DARE_N",length:27,filter:1.5});
assert.deepEqual(normalized,{enabled:true,pair:"EUR_NZD",timeframe:"S5",indicator:"DARE_N",length:27,filter:1.5});
assert.equal(normalizeIndicatorOnlyUnits(undefined),100);assert.equal(normalizeIndicatorOnlyUnits(2500),2500);assert.equal(normalizeIndicatorOnlyUnits(0),100);assert.equal(normalizeIndicatorOnlyUnits(200000000),100000000);
assert.equal(ioClientOrderId("EUR_USD","event-a"),ioClientOrderId("EUR_USD","event-a"));assert.notEqual(ioClientOrderId("EUR_USD","event-a"),ioClientOrderId("EUR_USD","event-b"));
assert.equal(indicatorOnlyCadenceMs("S5"),5000);assert.equal(indicatorOnlyCadenceMs("S30"),30000);assert.equal(indicatorOnlyCadenceMs("M1"),60000);assert.equal(indicatorOnlyCadenceMs("H1"),60000);assert.notEqual(indicatorOnlyFingerprint(normalized),indicatorOnlyFingerprint({...normalized,pair:"EUR_USD"}));
const dareN=indicatorOnlySettings(normalized);assert.equal(dareN.dareNLength,27);assert.equal(dareN.dareNFilter,1.5);assert.equal(indicatorOnlySettings({indicator:"ASSET",length:31}).assetLength,31);assert.equal(indicatorOnlySettings({indicator:"NAI",length:19,filter:.5}).naiFilter,.5);assert.equal(indicatorOnlySettings({indicator:"APEX",length:13,filter:3}).apexFilter,3);

const ticket1=normalizeTicket({enabled:true,pair:"EUR_USD",timeframe:"M30",indicator:"ASSET",length:20,filter:0,units:1000},1),ticket2=normalizeTicket({enabled:true,pair:"GBP_USD",timeframe:"S5",indicator:"NAI",length:30,filter:1,units:2000},2);
assert.deepEqual(normalizeTickets([ticket1,ticket2],{}),[ticket1,ticket2]);assert.throws(()=>normalizeTickets([ticket1,{...ticket2,pair:"EUR_USD"}],{}),/two separate currency pairs/);
assert.equal(eventIsFresh({bars:1}),true);assert.equal(eventIsFresh({bars:2}),false);assert.equal(eventIsFresh({bars:8}),false);assert.equal(eventObservedAt({startTime:"2026-08-10T09:30:00.000Z"},"M30"),"2026-08-10T10:00:00.000Z");assert.equal(ticketCadenceMs(ticket1),60000);assert.equal(ticketCadenceMs(ticket2),5000);assert.notEqual(structuralFingerprint(ticket1),structuralFingerprint({...ticket1,length:30}));

const engine=fs.readFileSync(new URL("../src/engine-indicator-only.js",import.meta.url),"utf8");assert.match(engine,/if\(!control\.enabled\)return super\.tick\(\)/);assert.match(engine,/if\(normalizeIndicatorOnly\(state\?\.indicatorOnly\)\.enabled\)return;/);assert.match(engine,/state\.pendingReversals=\{\}/);assert.doesNotMatch(engine,/ageMarketWindow|reallocationDecision|continuationExpectation|this\.choose\(/);
const unitsEngine=fs.readFileSync(new URL("../src/engine-indicator-only-units.js",import.meta.url),"utf8");assert.match(unitsEngine,/class HtlEngine extends IndicatorOnlyEngine/);assert.match(unitsEngine,/requested>available/);assert.match(unitsEngine,/units:String\(signed\)/);assert.doesNotMatch(unitsEngine,/safeCapacity|marginAvailable/);
const dual=fs.readFileSync(new URL("../src/engine-indicator-only-dual.js",import.meta.url),"utf8");assert.match(dual,/INDICATOR_ONLY_DUAL@1\.0\.0/);assert.match(dual,/indicatorOnlyTickets/);assert.match(dual,/Indicator Only tickets must use two separate currency pairs/);assert.match(dual,/Disengage IO Ticket \$\{index\+1\} before changing pair, timeframe, indicator, length, or filter/);assert.match(dual,/if\(!eventIsFresh\(event\)\)/);assert.match(dual,/INDICATOR_ONLY_STALE_SIGNAL/);assert.match(dual,/eventObservedAt/);assert.match(dual,/INDICATOR_ONLY_EXECUTION_TIMING/);assert.match(dual,/executionDelayMs/);assert.match(dual,/runtime\.nextDue/);assert.match(dual,/Math\.min\(\.\.\.nextTimes\)/);assert.match(dual,/for\(const ticket of work\)\{try\{await this\.tickTicket/);assert.match(dual,/if\(activeTickets\(state\)\.length\)return;/);

const ui=fs.readFileSync(new URL("../public/indicator-only.js",import.meta.url),"utf8");assert.match(ui,/const suffix=slot=>slot===1\?"":String\(slot\)/);assert.match(ui,/const id=\(name,slot\)=>`indicatorOnly\$\{name\}\$\{suffix\(slot\)\}`/);assert.match(ui,/ticketMarkup\(1\)\}\$\{ticketMarkup\(2\)\}/);for(const name of ["Toggle","Pair","Timeframe","Indicator","Length","Filter","Units"])assert.match(ui,new RegExp(`id\\(\\"${name}\\",slot\\)`));assert.match(ui,/CTE_INDICATOR_ONLY_UI@2\.0\.0/);assert.match(ui,/Two Independent Execution Tickets/);assert.match(ui,/indicatorOnlyTickets:currentTickets\(\)/);assert.match(ui,/lockNormal\(source\.some\(item=>item\?\.enabled\)\)/);assert.match(ui,/setTicketLocked\(slot,enabled\)/);assert.match(ui,/executionDelayMs/);assert.doesNotMatch(ui,/chartPair|chartTimeframe|chartStrategy|chartLength|chartFilter/);assert.match(ui,/anchor\.insertAdjacentElement\(\"afterend\",root\)/,"Indicator Only tickets must sit directly below the canonical chart title");assert.match(ui,/root\.insertAdjacentElement\(\"afterend\",toolbar\)/,"Chart controls must sit directly below Indicator Only tickets");assert.match(ui,/toolbar\.dataset\.chartControlAttachment=\"canonical-chart\"/,"Chart toolbar must retain explicit canonical-chart attachment");assert.doesNotMatch(ui,/anchor\.appendChild\(root\)/,"Indicator Only tickets must not remain embedded in the chart header beside the toolbar");

const worker=fs.readFileSync(new URL("../src/worker.js",import.meta.url),"utf8"),closeRetry=fs.readFileSync(new URL("../src/engine-close-retry.js",import.meta.url),"utf8");assert.match(worker,/export \{ HtlEngine \} from "\.\/engine-close-retry\.js"/);assert.match(closeRetry,/class HtlEngine extends DualIndicatorOnlyEngine/);assert.match(worker,/indicatorOnlyAuthority\(env\)/);assert.match(worker,/ticketCount/);assert.match(worker,/Order route authority unavailable; order withheld/);
console.log("Indicator Only certification passed beneath the OANDA close retry guard: two independent tickets, exact-unit sizing, fresh-transition execution, per-ticket cadence/error isolation, and exclusive IO authority remain wired.");