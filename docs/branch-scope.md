# Branch scope

This branch repairs the Rate Fluctuation / Event Outcome corroboration contract and adds its focused regression to the mandatory repository check. It also records the standing Indicator Signal Chain Inspection and the HTL Schedule 28-pair market-table work order.

The HTL Schedule market-table application code and an execution-bound signal-price alias are not included in this branch because those repository writes were rejected by the active tool safety boundary. This branch must not be represented as implementing those two blocked application changes.
