import horizonWorker from "./worker-horizon-base.js";
import "../public/htl-horizon-contract.js";
export { HtlEngine } from "./engine.js";

const H = globalThis.CTE_HORIZON_HTL;
const API = "https://api-fxtrade.oanda.com";
const VERSION = H.VERSION;
const INSTRUMENTS = new Set(["EUR_USD","GBP_USD","USD_JPY","USD_CAD","USD_CHF","AUD_USD","NZD_USD","EUR_GBP","EUR_JPY","EUR_CHF","EUR_AUD","EUR_CAD","EUR_NZD","GBP_JPY","GBP_CHF","GBP_AUD","GBP_CAD","GBP_NZD","AUD_JPY","AUD_CHF","AUD_CAD","AUD_NZD","NZD_JPY","NZD_CHF","NZD_CAD","CAD_JPY","CAD_CHF","CHF_JPY"]);
const TIMEFRAMES = new Set(["W","D","H4","H1","M30","M15","M5","M1","S30","S5"]);
const STRATEGIES = new Set(["ASSET","DARE_N","DARE","COMBO","NAI","APEX"]);
const headers = { "Content-Type": "application/json; charset=utf-8", "Cache-Control": "no-store", "X-Content-Type-Options": "nosniff" };
const json = (value, status=200) => new Response(JSON.stringify(value), { status, headers });

function assertSameOrigin(request){const url=new URL(request.url),origin=request.headers.get("Origin"),site=request.headers.get("Sec-Fetch-Site");if(origin&&origin!==url.origin)throw Object.assign(new Error("Cross-origin request rejected."),{status:403});if(!origin&&!site)throw Object.assign(new Error("Browser-origin request required."),{status:403});if(site&&!['same-origin','same-site','none'].includes(site))throw Object.assign(new Error("Cross-site request rejected."),{status:403});}
function credentials(env){const token=String(env.OANDA_API_KEY||"").trim(),configured=String(env.OANDA_ACCOUNT_ID||"").trim();if(token.length<20||!configured)throw Object.assign(new Error("OANDA secrets unavailable."),{status:503});return{token,configured};}
async function callOanda(path,token,init={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),15000);try{const response=await fetch(API+path,{method:init.method||"GET",headers:{Authorization:`Bearer ${token}`,Accept:"application/json",...(init.body?{"Content-Type":"application/json"}:{})},body:init.body,redirect:"manual",cache:"no-store",signal:controller.signal}),payload=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(payload.errorMessage||payload.errorCode||`OANDA HTTP ${response.status}`),{status:response.status,payload});return payload;}catch(error){if(controller.signal.aborted)throw Object.assign(new Error("OANDA request timed out."),{status:504});throw error;}finally{clearTimeout(timer);}}
async function accountId(token,configured){const payload=await callOanda("/v3/accounts",token),accounts=payload.accounts||[],selected=accounts.find(account=>account.id===configured&&!account.tags?.includes("MT4"))||accounts.find(account=>String(account.id||"").endsWith("-001")&&!account.tags?.includes("MT4"));if(!selected?.id)throw Object.assign(new Error("Authorized non-MT4 account ending -001 not found."),{status:401});return selected.id;}
function normalizeCandles(payload){return(payload.candles||[]).filter(candle=>candle.complete===true&&candle.mid).map(candle=>({time:candle.time,open:Number(candle.mid.o),high:Number(candle.mid.h),low:Number(candle.mid.l),close:Number(candle.mid.c),complete:true})).filter(candle=>[candle.open,candle.high,candle.low,candle.close].every(Number.isFinite));}
function hash(value){let output=2166136261;for(const character of String(value)){output^=character.charCodeAt(0);output=Math.imul(output,16777619);}return(output>>>0).toString(16);}
function normalizeCandidateOrder(value){const order=value?.order||{},context=value?.cteContext||{},instrument=String(order.instrument||"").toUpperCase(),units=Number(order.units),timeframe=String(context.timeframe||"").toUpperCase(),strategy=String(context.strategy||"").toUpperCase(),length=Math.trunc(Number(context.length)),filter=Number(context.filter),direction=Number(context.rawDirection);if(!INSTRUMENTS.has(instrument)||context.pair!==instrument)throw Object.assign(new Error("Candidate pair does not match the order instrument."),{status:400});if(!Number.isInteger(units)||units===0)throw Object.assign(new Error("Candidate units must be a non-zero integer."),{status:400});if(order.type!=="MARKET"||order.timeInForce!=="FOK"||order.positionFill!=="DEFAULT")throw Object.assign(new Error("Only MARKET FOK DEFAULT candidate orders are permitted."),{status:400});if(!TIMEFRAMES.has(timeframe)||!STRATEGIES.has(strategy)||!Number.isInteger(length)||length<3||length>200||!Number.isFinite(filter)||filter<0||filter>10)throw Object.assign(new Error("Invalid candidate configuration identity."),{status:400});if(context.calculationVersion!==VERSION||!String(context.crossingIdentity||"").startsWith(`${VERSION}:`)||!context.crossingTime)throw Object.assign(new Error("Candidate crossing identity is not canonical."),{status:409});if(Math.sign(units)!==Math.sign(direction))throw Object.assign(new Error("Candidate order direction does not match its crossing."),{status:409});return{order:{instrument,units:String(units),type:"MARKET",timeInForce:"FOK",positionFill:"DEFAULT"},context:{...context,timeframe,strategy,length,filter,rawDirection:Math.sign(direction)}};}
async function verifyCrossing(candidate,token){const query=new URLSearchParams({price:"M",granularity:candidate.context.timeframe,count:"650",smooth:"false"}),candles=normalizeCandles(await callOanda(`/v3/instruments/${candidate.order.instrument}/candles?${query}`,token)),built=H.build(candles,candidate.context.length),crossing=built.crossings.find(event=>event.time===candidate.context.crossingTime&&event.direction===candidate.context.rawDirection);if(!crossing)throw Object.assign(new Error("The cited Horizon crossing is not present in current completed OANDA history."),{status:409});const identity=H.crossingIdentity({pair:candidate.order.instrument,timeframe:candidate.context.timeframe,strategy:candidate.context.strategy,length:candidate.context.length,filter:candidate.context.filter,crossing});if(identity!==candidate.context.crossingIdentity)throw Object.assign(new Error("The Horizon crossing identity changed during live revalidation."),{status:409});const sigma=built.assetDeviation[crossing.index],separation=Math.abs(crossing.asset-crossing.inverse),separationSigma=Number.isFinite(sigma)&&sigma>0?separation/sigma:candidate.context.filter<=0?Infinity:0;if(candidate.context.filter>0&&separationSigma<candidate.context.filter)throw Object.assign(new Error(`The raw crossing remains valid but no longer qualifies: ${separationSigma.toFixed(4)} < filter ${candidate.context.filter}.`),{status:409});return{crossing,identity,separation,separationSigma,lastCandle:candles.at(-1)?.time||null};}
async function handleCandidateOrder(request,env){const body=await request.json().catch(()=>null),candidate=normalizeCandidateOrder(body),{token,configured}=credentials(env),account=await accountId(token,configured),verification=await verifyCrossing(candidate,token),positionPayload=await callOanda(`/v3/accounts/${encodeURIComponent(account)}/positions/${candidate.order.instrument}`,token).catch(error=>Number(error?.status)===404?{position:null}:Promise.reject(error)),position=positionPayload.position,longUnits=Number(position?.long?.units||0),shortUnits=Math.abs(Number(position?.short?.units||0));if(longUnits||shortUnits)throw Object.assign(new Error("The currency pair already has an open OANDA position."),{status:409});const preferencesResponse=await env.HTL_ENGINE.getByName("live").fetch("https://engine/preferences"),preferences=await preferencesResponse.json().catch(()=>({})),minimumUnits=Math.max(1,Math.trunc(Number(preferences.minimumUnits)||1000),),requested=Math.abs(Number(candidate.order.units));if(requested<minimumUnits)throw Object.assign(new Error(`Requested units ${requested} are below minimum units ${minimumUnits}.`),{status:400});const pricing=await callOanda(`/v3/accounts/${encodeURIComponent(account)}/pricing?instruments=${candidate.order.instrument}&includeUnitsAvailable=true`,token),available=pricing.prices?.[0]?.unitsAvailable?.default,capacity=Math.max(0,Math.trunc(Number(Number(candidate.order.units)>0?available?.long:available?.short)||0));if(requested>capacity)throw Object.assign(new Error(`Requested units ${requested} exceed directional availability ${capacity}.`),{status:409});const clientId=`cte-hz-${hash(candidate.context.crossingIdentity)}`;try{const existing=await callOanda(`/v3/accounts/${encodeURIComponent(account)}/orders/@${encodeURIComponent(clientId)}`,token);if(existing?.order)throw Object.assign(new Error(`OANDA already contains candidate order ${clientId}; duplicate submission suppressed.`),{status:409});}catch(error){if(Number(error?.status)!==404)throw error;}candidate.order.clientExtensions={id:clientId,tag:"cte-horizon",comment:candidate.context.crossingIdentity.slice(0,128)};const result=await callOanda(`/v3/accounts/${encodeURIComponent(account)}/orders`,token,{method:"POST",body:JSON.stringify({order:candidate.order})}),fill=result.orderFillTransaction;if(!fill){const rejected=result.orderRejectTransaction||result.orderCancelTransaction;throw Object.assign(new Error(rejected?.rejectReason||rejected?.reason||"OANDA returned no order fill."),{status:409,payload:result});}const entry={type:"MANUAL_CANDIDATE_ORDER",pair:candidate.order.instrument,timeframe:candidate.context.timeframe,strategy:candidate.context.strategy,htlLength:candidate.context.length,filter:candidate.context.filter,calculationVersion:VERSION,configurationHash:hash(JSON.stringify({pair:candidate.order.instrument,timeframe:candidate.context.timeframe,strategy:candidate.context.strategy,length:candidate.context.length,filter:candidate.context.filter,version:VERSION})),crossingTime:verification.crossing.time,rawDirection:verification.crossing.direction,priorAsset:verification.crossing.priorAsset,priorInverse:verification.crossing.priorInverse,currentAsset:verification.crossing.asset,currentInverse:verification.crossing.inverse,qualificationResult:"QUALIFIED",qualificationReason:candidate.context.filter>0?`SEPARATION_SIGMA_${verification.separationSigma.toFixed(4)}_GTE_${candidate.context.filter}`:"RAW_CROSSING_NO_FILTER",nemotronRecommendedPair:candidate.context.nemotronRecommendedPair||null,nemotronSelected:Boolean(candidate.context.nemotronSelected),direction:Number(candidate.order.units)>0?"BUY":"SELL",units:Math.abs(Number(fill.units)||requested),price:fill.price||null,transaction:fill.id||result.lastTransactionID||null,clientOrderId:clientId,event:candidate.context.crossingIdentity,message:"A/B/C candidate order filled after live canonical crossing, position, units, and duplicate revalidation"};await env.HTL_ENGINE.getByName("live").fetch(new Request("https://engine/manual-trade-action",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(entry)}));return json({filled:true,transactionId:entry.transaction,clientOrderId:clientId,crossingIdentity:candidate.context.crossingIdentity,calculationVersion:VERSION,result});}

export const __candidateOrderTest=Object.freeze({normalizeCandidateOrder,verifyCrossing,VERSION});

export default{
  async fetch(request,env,ctx){const url=new URL(request.url);if(url.pathname==="/api/oanda/order"&&request.method==="POST"){const clone=request.clone(),body=await clone.json().catch(()=>null);if(body?.cteContext){try{assertSameOrigin(request);return await handleCandidateOrder(request,env);}catch(error){return json({error:error?.message||"Candidate order failed.",details:error?.payload||undefined},Number(error?.status)||500);}}}return horizonWorker.fetch(request,env,ctx);},
  async scheduled(event,env,ctx){return horizonWorker.scheduled(event,env,ctx);}
};
