import "../public/htl-horizon-contract.js";
import "../public/horizon-strategy-contract.js";

const H=globalThis.CTE_HORIZON_HTL,S=globalThis.CTE_HORIZON_STRATEGIES;
export const OPTIMIZER_VERSION=5;
export const VALIDATION="HORIZON_RETROSPECTIVE_PLATFORM_PARITY";
export const MAX_COMPUTE_BARS=2500;
export const OPTIMIZER_TTL_MS=7*24*60*60*1000;
export const PAIRS=["EUR_USD","GBP_USD","USD_JPY","USD_CAD","USD_CHF","AUD_USD","NZD_USD","EUR_GBP","EUR_JPY","EUR_CHF","EUR_AUD","EUR_CAD","EUR_NZD","GBP_JPY","GBP_CHF","GBP_AUD","GBP_CAD","GBP_NZD","AUD_JPY","AUD_CHF","AUD_CAD","AUD_NZD","NZD_JPY","NZD_CHF","NZD_CAD","CAD_JPY","CAD_CHF","CHF_JPY"];
export const TIMEFRAMES=["W","D","H4","H1","M30","M15","M5","M1","S30","S5"];
export const TIMEFRAME_SECONDS={W:604800,D:86400,H4:14400,H1:3600,M30:1800,M15:900,M5:300,M1:60,S30:30,S5:5};
export const STRATEGIES=["ASSET","DARE_N","DARE","COMBO","NAI","APEX"];
export const LENGTH_GRID=[10,20,30,50];
export const FILTER_GRID=Object.freeze({ASSET:[0],DARE_N:[0,.5,1,2],DARE:[0],COMBO:[0,.5,1,2],NAI:[0,.5,1,2],APEX:[0,1,2]});
const API="https://api-fxtrade.oanda.com";
const mean=values=>values.length?values.reduce((sum,value)=>sum+value,0)/values.length:0;
const responseError=(message,status=400)=>Object.assign(new Error(message),{status});

async function callOanda(path,token,init={}){const result=await fetch(API+path,{method:init.method||"GET",headers:{Authorization:`Bearer ${token}`,Accept:"application/json",...(init.body?{"Content-Type":"application/json"}:{})},body:init.body,redirect:"manual",cache:"no-store"}),payload=await result.json().catch(()=>({}));if(!result.ok)throw Object.assign(new Error(payload.errorMessage||payload.errorCode||`OANDA HTTP ${result.status}`),{status:result.status,payload});return payload;}
function token(env){const value=String(env.OANDA_API_KEY||"").trim();if(value.length<20)throw responseError("OANDA secrets unavailable.",503);return value;}
function normalizeCandles(payload){return(payload.candles||[]).filter(candle=>candle.complete===true&&candle.mid).map(candle=>({time:candle.time,open:Number(candle.mid.o),high:Number(candle.mid.h),low:Number(candle.mid.l),close:Number(candle.mid.c),volume:Number(candle.volume||0),complete:true})).filter(candle=>[candle.open,candle.high,candle.low,candle.close].every(Number.isFinite));}
export async function candles(pair,apiToken,timeframe,count=650){const query=new URLSearchParams({price:"M",granularity:timeframe,count:String(count),smooth:"false"});return normalizeCandles(await callOanda(`/v3/instruments/${pair}/candles?${query}`,apiToken));}
export async function candlesForRange(pair,apiToken,timeframe,startDate,endDate){const start=new Date(`${startDate}T00:00:00.000Z`),end=new Date(`${endDate}T23:59:59.999Z`);if(!Number.isFinite(start.getTime())||!Number.isFinite(end.getTime())||start>end)throw responseError("Invalid Compute Configuration date range.");const estimated=Math.ceil((end-start)/(TIMEFRAME_SECONDS[timeframe]*1000))+2;if(estimated>MAX_COMPUTE_BARS)throw responseError(`Selected ${timeframe} range is too large (${estimated.toLocaleString()} estimated bars; maximum ${MAX_COMPUTE_BARS.toLocaleString()}).`);const query=new URLSearchParams({price:"M",granularity:timeframe,from:start.toISOString(),to:end.toISOString(),smooth:"false",includeFirst:"true"}),data=normalizeCandles(await callOanda(`/v3/instruments/${pair}/candles?${query}`,apiToken));if(data.length>MAX_COMPUTE_BARS)throw responseError(`OANDA returned ${data.length.toLocaleString()} bars; maximum ${MAX_COMPUTE_BARS.toLocaleString()}.`);return data;}

export function currentEvent(data,pair,timeframe,length,strategy="ASSET",filter=0){const active=S.active(data,length,strategy,filter);if(!active)return null;const crossing=active.rawCrossing,start=data[active.index],id=H.crossingIdentity({pair,timeframe,strategy,length,filter,crossing});return{direction:active.direction,rawDirection:active.direction,startTime:active.qualified?start.time:null,crossingTime:start.time,openPrice:start.close,bars:data.length-active.index,id,qualified:active.qualified,qualificationResult:active.qualified?"QUALIFIED":"REJECTED",qualificationReason:active.qualificationReason,qualificationMetrics:active.qualificationMetrics,calculationVersion:H.VERSION,qualificationVersion:S.VERSION,priorAsset:crossing.priorAsset,priorInverse:crossing.priorInverse,asset:crossing.asset,inverse:crossing.inverse,crossing:{index:crossing.index,time:crossing.time,direction:crossing.direction,priorAsset:crossing.priorAsset,priorInverse:crossing.priorInverse,asset:crossing.asset,inverse:crossing.inverse}};}

function walkForwardScore(data,events,pair){const pip=pair.endsWith("JPY")?100:10000,foldStarts=[.4,.55,.7,.85].map(value=>Math.floor(data.length*value)),foldEnds=[...foldStarts.slice(1),data.length],trades=[];for(let index=0;index<events.length-1;index++){const current=events[index],next=events[index+1],entryIndex=current.index+1,exitIndex=next.index+1;if(!current.qualified||entryIndex>=data.length||exitIndex>=data.length||exitIndex<=entryIndex)continue;if(!foldStarts.some((start,fold)=>entryIndex>=start&&exitIndex<foldEnds[fold]))continue;const entry=data[entryIndex].open,exit=data[exitIndex].open,range=data.slice(entryIndex,exitIndex+1),gross=(exit-entry)*current.direction*pip,net=gross-1,mfe=(current.direction>0?Math.max(...range.map(candle=>candle.high))-entry:entry-Math.min(...range.map(candle=>candle.low)))*pip,mae=(current.direction>0?entry-Math.min(...range.map(candle=>candle.low)):Math.max(...range.map(candle=>candle.high))-entry)*pip;trades.push({net,mfe:Math.max(0,mfe),mae:Math.max(0,mae)});}const wins=trades.filter(trade=>trade.net>.05),losses=trades.filter(trade=>trade.net<-.05),flats=trades.length-wins.length-losses.length,net=trades.reduce((sum,trade)=>sum+trade.net,0),average=trades.length?net/trades.length:0,grossWin=wins.reduce((sum,trade)=>sum+trade.net,0),grossLoss=-losses.reduce((sum,trade)=>sum+trade.net,0);let equity=0,peak=0,maxDrawdown=0;for(const trade of trades){equity+=trade.net;peak=Math.max(peak,equity);maxDrawdown=Math.max(maxDrawdown,peak-equity);}const variance=trades.length?mean(trades.map(trade=>(trade.net-average)**2)):0,uncertainty=Math.sqrt(variance/Math.max(1,trades.length)),mfe=trades.reduce((sum,trade)=>sum+trade.mfe,0),mae=trades.reduce((sum,trade)=>sum+trade.mae,0);return{trades:trades.length,wins:wins.length,losses:losses.length,flats,net,average,mfeMae:mae?mfe/mae:mfe?Infinity:null,maxDrawdown,profitFactor:grossLoss?grossWin/grossLoss:grossWin?Infinity:null,recoveryFactor:maxDrawdown?net/maxDrawdown:net>0?Infinity:null,winRate:trades.length?wins.length/trades.length:0,score:trades.length>=5?net-.5*maxDrawdown-uncertainty:-Infinity,estimatedCostPips:1,validation:VALIDATION};}
export function optimizeDataset(data,pair){const lengths=LENGTH_GRID.filter(length=>length*3<data.length),config={};for(const strategy of STRATEGIES){let best={length:lengths[0]||10,filter:FILTER_GRID[strategy][0],score:-Infinity,trades:0,net:0,maxDrawdown:0,winRate:0,validation:VALIDATION};for(const length of lengths)for(const filter of FILTER_GRID[strategy]){const stats=walkForwardScore(data,S.events(data,length,strategy,filter),pair);if(stats.score>best.score)best={length,filter,...stats};}config[strategy]={...best,candidateLengths:[...lengths],candidateFilters:[...FILTER_GRID[strategy]],calculationVersion:H.VERSION,qualificationVersion:S.VERSION};}return config;}
export function currentOptimizer(records){const now=Date.now();return Object.fromEntries(Object.entries(records||{}).filter(([,record])=>record?.version===OPTIMIZER_VERSION&&record?.calculationVersion===H.VERSION&&record?.qualificationVersion===S.VERSION&&now-Date.parse(record.computedAt||record.stamp||0)<OPTIMIZER_TTL_MS));}

export async function computeConfiguration(engine,value={}){let stage="validation";try{const pair=String(value.pair||"").toUpperCase(),timeframe=String(value.timeframe||"").toUpperCase(),startDate=String(value.startDate||""),endDate=String(value.endDate||""),hasDateRange=Boolean(startDate||endDate);if(!PAIRS.includes(pair))throw responseError("Invalid Compute Configuration currency pair.");if(!TIMEFRAMES.includes(timeframe))throw responseError("Invalid Compute Configuration timeframe.");if(hasDateRange&&(!/^\d{4}-\d{2}-\d{2}$/.test(startDate)||!/^\d{4}-\d{2}-\d{2}$/.test(endDate)))throw responseError("Both dates are required when an explicit range is supplied.");stage="credentials";const apiToken=token(engine.env);stage="oanda-history";const data=hasDateRange?await candlesForRange(pair,apiToken,timeframe,startDate,endDate):await candles(pair,apiToken,timeframe);if(data.length<80)throw responseError(`Insufficient completed candles for Compute Configuration: ${data.length}.`);stage="horizon-parity-optimization";const config=optimizeDataset(data,pair),stamp=data.at(-1)?.time||new Date().toISOString();stage="durable-storage";const records=(await engine.ctx.storage.get("optimizer"))||{},key=`${pair}|${timeframe}`,record={version:OPTIMIZER_VERSION,calculationVersion:H.VERSION,qualificationVersion:S.VERSION,stamp,computedAt:new Date().toISOString(),source:"COMPUTE_CONFIGURATION",validation:VALIDATION,modelComparison:{horizon:{crossing:H.VERSION,configurationOwnership:"SHARED_ASSET_LENGTH_IN_ORIGINAL_MODEL"},compound:{crossing:H.VERSION,qualification:S.VERSION,configurationOwnership:"STRATEGY_SCOPED_IMMUTABLE_RECORDS"},parity:"ONE_RAW_ASSET_INVERSE_CROSSING_CLOCK_WITH_STRATEGY_QUALIFICATION"},range:{startDate:hasDateRange?startDate:null,endDate:hasDateRange?endDate:null,firstCandle:data[0]?.time||null,lastCandle:data.at(-1)?.time||null,bars:data.length},config};records[key]=record;await engine.ctx.storage.put("optimizer",records);return{key,record};}catch(error){if(!error.stage)error.stage=stage;throw error;}}
export async function optimizeNext(engine,state,apiToken){const total=PAIRS.length*TIMEFRAMES.length,index=Number(state.optimizerCycleIndex||0)%total,pair=PAIRS[index%PAIRS.length],timeframe=TIMEFRAMES[Math.floor(index/PAIRS.length)],key=`${pair}|${timeframe}`,records=(await engine.ctx.storage.get("optimizer"))||{},existing=records[key];state.optimizerCycleIndex=(index+1)%total;if(existing?.version===OPTIMIZER_VERSION&&existing?.calculationVersion===H.VERSION&&existing?.qualificationVersion===S.VERSION&&existing?.source==="COMPUTE_CONFIGURATION"&&Date.now()-Date.parse(existing.computedAt||0)<OPTIMIZER_TTL_MS){state.optimizerLastDataset=key;state.optimizerLastRun=new Date().toISOString();state.optimizerLastError=null;return{records:currentOptimizer(records),key,record:existing};}const data=await candles(pair,apiToken,timeframe),config=optimizeDataset(data,pair),stamp=data.at(-1)?.time||new Date().toISOString(),record={version:OPTIMIZER_VERSION,calculationVersion:H.VERSION,qualificationVersion:S.VERSION,stamp,computedAt:new Date().toISOString(),source:"SERVER",validation:VALIDATION,range:{startDate:null,endDate:null,firstCandle:data[0]?.time||null,lastCandle:data.at(-1)?.time||null,bars:data.length},config};records[key]=record;await engine.ctx.storage.put("optimizer",records);state.optimizerLastDataset=key;state.optimizerLastRun=new Date().toISOString();state.optimizerLastError=null;return{records:currentOptimizer(records),key,record};}
export async function scan(engine,apiToken,config,timeframe=config.timeframe,optimizer={}){const rows=[],errors=[];for(const pair of PAIRS)try{const effective=engine.pairConfig(config,optimizer,pair,timeframe),data=await candles(pair,apiToken,timeframe),event=currentEvent(data,pair,timeframe,effective.primary.length,effective.strategy,effective.primary.filter),confirmation=effective.confirmationStrategy==="NONE"?null:currentEvent(data,pair,timeframe,effective.confirmation.length,effective.confirmationStrategy,effective.confirmation.filter);if(event){if(confirmation&&(!confirmation.qualified||confirmation.direction!==event.direction||confirmation.crossingTime!==event.crossingTime)){event.startTime=null;event.qualified=false;event.qualificationResult="REJECTED";event.qualificationReason=`CONFIRMATION_NOT_ALIGNED__${confirmation.qualificationReason}`;}rows.push({pair,event,configuration:{primary:effective.primary,confirmation:effective.confirmation,calculationVersion:H.VERSION,qualificationVersion:S.VERSION}});}}catch(error){errors.push({pair,error:String(error?.message||error)});}if(errors.length)await engine.write({type:"SCAN_PARTIAL",timeframe,calculationVersion:H.VERSION,qualificationVersion:S.VERSION,message:`${timeframe}: ${rows.length} pairs loaded, ${errors.length} failed`,failures:errors},false);return rows;}

export const __platformTest=Object.freeze({buildIndicators:S.buildIndicators,strategyEvents:S.events,currentEvent,optimizeDataset,VALIDATION,OPTIMIZER_VERSION,calculationVersion:H.VERSION,qualificationVersion:S.VERSION});
