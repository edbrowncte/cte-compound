name: Finalize incremental causal HTL repair

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  finalize:
    if: github.head_ref == 'repair/no-data-orchestration'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: repair/no-data-orchestration
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply incremental causal HTL repair
        run: python scripts/apply-fast-causal-htl.py
      - name: Execute complete repository checks
        run: npm run check
      - name: Commit verified source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Replace prefix HTL rebuilds with incremental causal reconstruction"
          git push origin HEAD:repair/no-data-orchestration

# Registered workflow revision.
